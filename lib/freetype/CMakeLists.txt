file(GLOB_RECURSE FREETYPE_HEADERS "${CMAKE_CURRENT_LIST_DIR}/include/freetype/*.h")
file(GLOB_RECURSE FT2BUILD_HEADER "${CMAKE_CURRENT_LIST_DIR}/include/ft2build.h")
file(GLOB_RECURSE FREETYPE_CONFIG_HEADERS "${CMAKE_CURRENT_LIST_DIR}/include/freetype/config/*.h")

list(APPEND HEADER_FILES ${FREETYPE_HEADERS} ${FT2BUILD_HEADER} ${FREETYPE_CONFIG_HEADERS})

target_sources(${PROJECT_NAME} PRIVATE ${HEADER_FILES})
target_include_directories(${PROJECT_NAME} PUBLIC "${CMAKE_CURRENT_LIST_DIR}/include")