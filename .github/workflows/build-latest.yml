name: C/C++ CI

on:
   workflow_dispatch:
jobs:
  ubuntu-build:
    runs-on: windows-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: CMAKE Cache
      run:
         cd dll && cmake . 
    - name: Build
      run:
         cmake . --build
